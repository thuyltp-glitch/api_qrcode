export enum TABLE_STATUS {
  AVAILABLE = 'Available',
  UNAVAILABLE = 'Unavailable',
}

export enum SESSION_STATUS {
  OPEN = 'Open',
  CLOSED = 'Closed',
}

export enum CART_STATUS {
  ACTIVE = 'Active',
  SUBMITTED = 'Submitted',
  CLOSED = 'Closed',
}
